{
  "meta": {
    "schema_version": "1.1.0",
    "awf_version": "3.3.0"
  },
  "project": {
    "name": "CarFloat",
    "type": "Android App",
    "status": "Active"
  },
  "tech_stack": {
    "frontend": {
      "framework": "Jetpack Compose",
      "language": "Kotlin"
    },
    "architecture": "Service-based Overlay"
  },
  "features": [
    "Floating Widgets (Clock, Weather, Voice, Status)",
    "Click-Through Gestures",
    "Auto Split-Screen",
    "Internationalization (8 languages)",
    "Non-intrusive OTA Update System via GitHub Releases"
  ],
  "knowledge_items": {
    "gotchas": [
      "System Alert Window permission is required for overlays.",
      "OTA Downloads must use context.getExternalFilesDir to avoid Permission Denied on some head units.",
      "BroadcastReceiver for DownloadManager MUST use applicationContext to prevent memory leaks during config changes.",
      "FileProvider needs broader paths config for diverse Android Automotive OS versions."
    ]
  }
}
